{
  "jobs": [
    {
      "functionLocation": "/square-scheduled-sync.js",
      "functionName": "scheduledInventorySync",
      "description": "Syncs inventory counts from Square to Wix every hour",
      "executionConfig": {
        "cronExpression": "0 * * * *"
      }
    },
    {
      "functionLocation": "/square-scheduled-sync.js",
      "functionName": "scheduledFullSync",
      "description": "Full product and inventory sync from Square to Wix every hour",
      "executionConfig": {
        "cronExpression": "30 * * * *"
      }
    }
  ]
}
